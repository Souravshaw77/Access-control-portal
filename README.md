# Access-control-portal

Minimal Django project scaffold for the Access Control Portal.

## Run locally

```bash
python -m pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```